#include "_macros.fos"

import void _TraderInit(Critter& boss, bool firstTime) from "trader";

void _DuppoInit(Critter& dappo, bool firstTime)
{
	
	_TraderInit(dappo, firstTime);
	// караваны будут новые
}

